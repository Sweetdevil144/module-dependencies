Images containing data for `modules` and their respective `dependencies` in a **Graph** and **Tabular** format

This repository contains resources utilised by my for demonstration within my GSoC Proposal.
